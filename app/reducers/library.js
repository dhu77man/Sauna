export default function(state={}, action) {

    function getLibraryFromSteam(){

    }

    switch(action.type){
        case 'LOAD_SAVED_LIBRARY':
            //let data = {}; // get saved library here
            //state = Object.assign({}, state, data);
            break;
        case 'PULL_LIBRARY':
            //let data = {}; // get saved library here
            //state = Object.assign({}, state, data);
            break;
    }
    

    return state;
}